<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<%= link_to "back to list", tasks_path %>

<%= link_to "Delete", task_delete_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
